Act as an expert frontend developer. Create a clean, modern, and production-ready HTML and CSS layout for a "Venue & Logistics" section based on the following structural and visual requirements:

1. Layout Structure (Flexbox):
   - The section should have a centered header with the title "The Venue & Logistics" and a subtitle below it.
   - Below the header, use a two-column layout using Flexbox that wraps into a single column on tablet/mobile screens.

2. Left Column (Information Card):
   - A white background card with rounded corners, a subtle gray border, and ample padding.
   - Inside the card: A small uppercase blue tag ("CONFERENCE HUB"), a bold heading ("Grand Pavilion Tech Centre"), and a muted sub-address text.
   - A vertical list of 3 logistics items. Each item must contain:
     * A light blue square background containing a FontAwesome icon (Train, Car, and Hotel respectively).
     * A bold title next to the icon (e.g., "By Public Transit", "Parking & Valet", "Hotel Partners").
     * A short paragraph of muted description text underneath the item title.

3. Right Column (Visual Map/Image Card):
   - An image container featuring a dark conference hall background, styled with rounded corners to match the left card.
   - It must include a centered overlay button with a vibrant blue background color (`#0ea5e9`) and bold white text that reads "Open in Google Maps".

4. Styling & Cleanliness:
   - Use clean, semantic HTML5 tags.
   - Write optimized, reusable CSS without duplicate properties. Ensure all spacing (margins/padding), font colors (dark text vs. muted gray text), and rounded corners perfectly match the professional UI in the description.